[package]
name = "ryu-sidecar-runtime"
version = "0.2.6"
edition = "2021"
license = "Apache-2.0"
description = "Shared Ryu sidecar process-boundary helpers"
repository = "https://github.com/amajorai/ryu"

[dependencies]
dirs = "5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
